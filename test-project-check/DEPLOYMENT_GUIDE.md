# JOKSHU Voting App - নতুন প্রজেক্ট

## 🎯 এই প্রজেক্টের ফিচার

✅ **সম্পূর্ণ JOKSHU ভোটিং সিস্টেম**
✅ **Expo.dev এর জন্য অপ্টিমাইজড**
✅ **APK বিল্ড রেডি**
✅ **React Native + TypeScript**
✅ **সম্পূর্ণ বাংলা UI সাপোর্ট**

## 🚀 শুরু করার নির্দেশনা

### পরিবেশ সেট আপ
```bash
# Node.js ইনস্টল করুন (v16+)
# Expo CLI ইনস্টল করুন
npm install -g @expo/eas-cli
npm install -g expo-cli
```

### প্রজেক্ট চালু করুন

1. **ডিপেন্ডেন্সি ইনস্টল করুন:**
```bash
cd "c:\Users\LP\Downloads\Compressed\JOKSHU-New"
npm install
```

2. **ডেভেলপমেন্ট সার্ভার চালু করুন:**
```bash
npx expo start
```

3. **QR কোড স্ক্যান করুন Expo Go অ্যাপ দিয়ে**

## 📱 APK তৈরি করার নির্দেশনা

### EAS বিল্ড সেট আপ
```bash
# Expo অ্যাকাউন্টে লগইন
eas login

# প্রজেক্ট কনফিগার করুন
eas build:configure
```

### APK বিল্ড করুন
```bash
# প্রিভিউ APK (টেস্টিং এর জন্য)
eas build --platform android --profile preview

# প্রোডাকশন AAB (Play Store এর জন্য)
eas build --platform android --profile production
```

## 🌐 Expo.dev এ আপলোড করার নির্দেশনা

### প্রজেক্ট পাবলিশ করুন
```bash
# Expo.dev এ পাবলিশ করুন
expo publish

# বা নতুন আপডেট পাঠান
eas update
```

### অনলাইন লিংক পাবেন:
- অ্যাপ: `https://expo.dev/@yourusername/jokshu-voting-new`
- QR কোড ব্যবহার করে মোবাইলে টেস্ট করুন

## 🔑 ডেমো লগইন ক্রেডেনশিয়াল

### ছাত্র লগইন:
- **Student ID**: `2022331001`
- **Password**: `123456`

### অ্যাডমিন লগইন:
- **Student ID**: `admin`
- **Password**: `admin123`

## 📁 প্রজেক্ট স্ট্রাকচার
```
JOKSHU-New/
├── App.tsx                 # মেইন অ্যাপ ফাইল
├── app.json                # Expo কনফিগ (Upload Ready)
├── eas.json                # বিল্ড কনফিগ (APK Ready)
├── package.json            # ডিপেন্ডেন্সি
├── src/
│   ├── components/         # UI কম্পোনেন্ট
│   ├── context/           # State ম্যানেজমেন্ট
│   ├── data/              # Mock ডেটা
│   ├── navigation/        # নেভিগেশন
│   ├── screens/           # সব স্ক্রিন
│   └── types/             # TypeScript টাইপ
└── assets/                # ইমেজ, আইকন
```

## ⚙️ গুরুত্বপূর্ণ সেটিংস

### 1. app.json এ পরিবর্তন:
- `name`: অ্যাপের নাম
- `slug`: ইউনিক স্ল্যাগ
- `bundleIdentifier` / `package`: ইউনিক আইডি

### 2. Assets আপডেট:
```bash
# এই ফাইলগুলো দিয়ে প্রতিস্থাপন করুন:
assets/icon.png           # 512x512px
assets/splash.png         # 1242x2436px
assets/adaptive-icon.png  # 1024x1024px
```

### 3. EAS প্রজেক্ট ID:
```bash
# নতুন প্রজেক্ট ID পেতে:
eas project:init
```

## 🔧 ট্রাবলশুটিং

### সমস্যা: Metro bundler error
```bash
npx expo start --clear
```

### সমস্যা: Build failed
```bash
# ক্যাশ ক্লিয়ার করুন
npm start -- --reset-cache
eas build --clear-cache
```

### সমস্যা: Dependencies conflict
```bash
# Clean install
rm -rf node_modules
rm package-lock.json
npm install
```

## 📊 বিল্ড স্ট্যাটাস চেক করুন

- EAS Dashboard: https://expo.dev/builds
- আপনার প্রজেক্ট: https://expo.dev/@yourusername/jokshu-voting-new

## 🚀 প্রোডাকশন রিলিজ

1. **Play Store এর জন্য:**
   - Production AAB বিল্ড করুন
   - Google Play Console এ আপলোড করুন

2. **সাইডলোডিং এর জন্য:**
   - Preview APK বিল্ড করুন
   - সরাসরি ইনস্টল করুন

## 📞 সাপোর্ট

সমস্যার জন্য:
1. Expo Documentation: https://docs.expo.dev/
2. EAS Build Guide: https://docs.expo.dev/build/introduction/

---

**জয় জগন্নাথ! 🎓**
*Jagannath University, Dhaka*